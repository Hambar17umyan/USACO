#include <fstream>
#include <algorithm>
#include <iostream>
#include <string>
#include <map>
using namespace std;

string y[14]= {"Ox", "Tiger", "Rabbit", "Dragon", "Snake", "Horse", "Goat", "Monkey", "Rooster", "Dog", "Pig", "Rat"};
int main()
{
	int i, j, k, l, m, n, o, p;
	cin >> n;
	map<string, int> mp;
	for (i = 0; i < n; i++)
	{
		string a, b, c, d;
		cin >> a;
		for (j = 0; j < 3; j++)
			cin >> b;
		cin >> c;
		for (j = 0; j < 3; j++)
			cin >> d;

	}

	return 0;
}